{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Python: Current File",
			"type": "python",
			"request": "launch",
			"program": "${file}",
			"console": "integratedTerminal",
			"cwd": "${fileDirname}",
			"justMyCode": true,
			"args": [
				"--data_dir", "/home/john/PhD/BISCUIT/data_generation/data/gridworld",
				"--num_latents", "40",
				// "--autoencoder_checkpoint", "/home/john/PhD/BISCUIT/pretrained_models/AE_40l_64hid.ckpt",
				// "--num_latents", "40",
				// "--c_hid", "64",
				// "--num_flows", "6",
				// "--lr", "1e-3",
				// "--num_samples", "2",
				"--batch_size", "8",
				// "--seed", "42",
				// "--text",
				// "--lr_text", "1e-2",
				// "--text_encoder", "siglip",
				"--subsample_percentage", "0.05",
				// "--max_epochs", "40",
				// "--seq_len", "2",
				// "--num_workers", "4",
				// // "--debug_data",
				// "--prior_action_add_prev_state"
				"--use_coordconv",

			]
		}
	]
}